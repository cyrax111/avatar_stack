name: example
description: An example for stack_avatar package

publish_to: 'none' 

version: 1.0.0+3

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  avatar_stack:
    path: ..

  cupertino_icons: ^1.0.4
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/avatar/