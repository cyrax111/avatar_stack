name: avatar_stack
description: Advanced and agile avatar stack with settings. It allows to place a lot of avatars or another widgets in small place.
maintainer: Alexander Bosak (bskaxe@gmail.com)
issue_tracker: https://github.com/cyrax111/avatar_stack/issues
repository: https://github.com/cyrax111/avatar_stack
version: 3.0.0

environment:
  sdk: ^3.4.3
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter: